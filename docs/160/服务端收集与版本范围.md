# 苍生铸世服务端收集与版本范围

## 分段

以战斗开始时间划分，使用 Unix 秒和左闭右开区间，显示时区为北京时间 UTC+8。上传时间不参与分段。

| 编号 | 名称 | 开始（包含） | 结束（不包含） |
| --- | --- | --- | --- |
| 160 | 苍生铸世（测试） | 本季测试记录 | 2026-10-29 07:00 |
| 161 | 苍生铸世（初版） | 2026-10-29 07:00 | 2026-12-21 07:00 |
| 162 | 苍生铸世（一削） | 2026-12-21 07:00，预计 | 当前时间模型上限 |

对应边界时间戳为 `1793228400` 和 `1797807600`，三个难度地图为 828、835、836。一削时间为用户确认的预计排期，实际改期时调整 `tools/Names.py`。此表定义统计阶段，不代表未来技改已经解包或适配。

## 数据入口

- `MAP_DICT_RECORD_LOGS` 为三难度启用收集，系数沿用 1/2/4。
- 整场战斗 `ActorStat.mapdetail` 存数字地图 ID；单人复盘 `ReplayProStat.mapdetail` 保持中文地图名。这是现有查询接口约定。
- 两类记录由服务端依据战斗时间计算 `gameEdition`；不同阶段的百分位键独立，客户端提交时间或服务器时区不会改变分段。
- `/getGameEditionFromMap` 保留 `result` 版本名称映射，并增加 `current`、`ranges`、`timezone`。`current` 根据当前时间计算，不提前选用未来的 162。
- `/uploadCombinedData`、`/uploadActorData`、`/uploadReplayPro` 在成功时提交，异常时回滚数据库并关闭连接；单人入口已修复多传参数导致的失败。

## 排行口径

160/161/162 的百分位样本及公开排行榜要求正式客户端至少 `8.16.0`。旧客户端和 beta 上传仍可保留为历史记录，但不进入新季排行。旧季百分位保留原有最低 `8.13.0` 范围，旧季公开列表规则不变。

后台排行更新与列表接口共用版本判定；版本元数据修正不重算旧客户端统计，也不把旧 rDPS 标记为已校准。

## 运行与发布

- 服务目录：`/home/ubuntu/jx3bla`，解释器 `.venv/bin/python`。
- 原窗口：`serverRestart:5`，pane `%34`，命令 `.venv/bin/python server.py`。
- 部署前 GitHub 已是 `origin`：`git@github.com:moeheart/jx3bla.git`，无需从 Gitee 切换。
- 部署前版本 `74d12ab` / `dev-8.12`，线上进程使用 8.15.5。配置、相关源文件及待更正的 125 份复盘、5 场战斗在 `/home/ubuntu/jx3bla_deployments/8.16.0-20260920T085845Z` 备份。
- 仅使用原 tmux pane 停止与重启服务；保留 `.venv`、`settings.cfg`、数据库文件目录及现有每日排行 cron。

本次本地 104 项单测通过，其中服务端收集 11 项、版本边界 6 项、排行门槛 8 项。测试覆盖三个难度、两边界前后、三个上传入口提交/失败回滚、地图及版本 API、正式客户端与旧客户端的排行隔离。

## 上线核验（2026-09-20）

- 已部署代码 `8940333`，在原 pane `%34` 重启；shell PID 284656 保持不变，服务进程 PID 由 307007 变为 721185。
- 服务器 Python 3.12.3 环境通过 51 项苍生铸世测试、11 项服务端收集测试。另对真实 InnoDB 数据库执行三个难度 × 三阶段共 9 组收集函数测试，全部成功后回滚；复盘编号计数器恢复，测试记录零残留，测试文件写入备份目录，未触碰正式复盘文件。
- 更正 125 份旧客户端复盘的 `gameEdition: 0 → 160`，以及关联 5 场战斗的 `mapdetail: 0 → 835`、`gameEdition: 0 → 160`。逐字段验证其余数值、客户端版本、玩家积分不变；即时备份保存在同一部署备份目录。
- 公网地图、五首领、阶段范围接口通过；当前三个难度均返回 160，空批量上传返回成功。旧 8.15.5 数据不进入新季排行榜，当前灵素列表为空，等待 8.16.0 的正式上传。
- GitHub origin、原配置哈希、每日排行 cron 及所有其他 tmux pane 保持一致。未重建或清空全季百分位表。
- 本地证据：`backups/server-8.16.0/deployment-evidence.json`；服务器详细证据与备份同目录。收集测试为真实函数和数据库事务验证，不等同于玩家客户端经公网完成正式上传。

## 下载公告发布（2026-09-20）

数据收集部署完成后，用户上传安装包并提供 [8.16.0 下载链接](https://wwbry.lanzouu.com/iINpr49a7v6d)。北京时间 18:04（UTC `2026-09-20T10:04:43.524620+00:00`）完成独立公告发布，更新 `PreloadInfo` 的版本、公告及下载地址，`rateEdition` 仍为 `1789848879`。

公网 `/getAnnouncement?edition=8.15.5` 和 `?edition=8.16.0` 均返回版本 `8.16.0`、当前 `Constants.py` 公告及新链接的 `updateurl` / `url`。旧客户端更新提示依据接口响应和现有版本比较逻辑核验，未启动旧 EXE 做交互验收。浏览器分享页文件名为 `剑三警长v8.16.0.exe`、大小 `49.7M`；下载组件超时，未重新下载或验证云端文件哈希。

原公告备份保存在部署备份目录的 `announcement-before-20260920T100345Z.json`；本地证据为 `backups/server-8.16.0/announcement-published.json`。此公告步骤与上述数据收集部署分别完成。
