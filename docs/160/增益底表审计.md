# 苍生铸世体服增益底表审计

## 数据来源与版本隔离

- 新季版本标识：`gameEdition=160`。8.16.0 发布使用客户端 `zhcn_exp 1.6.0.9506` 于 **2026-09-20** 提取的 14 份核心表。
- 发布用副本来自 `C:/Develop/26/jx3decrypt/reports/luoyangzhizhan-0920-check/source/critical_files`；已逐份核对该报告的 `validation/source_manifest.json`，也与当前 `critical_files/result_exp` 一致。原表保存在 `equip/resources/cangshengtf`，逐文件版本、来源、大小、时间戳、SHA256 见该目录的 `source_manifest.json`。
- 心法 50 级、NPC 防御、角色面板、原生镶嵌和参数 Lua 仍保留 **9503** 的既有取证，本次没有重新解包这些原件。顶层客户端版本只描述 14 份核心表；其他证据按各文件的版本与来源读取，不能据此说全部属性机制都已重新验证到 9506。
- 新运行表为 `replayer/NameCangsheng.py`。旧 `replayer/Name.py`、根目录装备源和历史赛季目录未覆盖。`CombatTracker` 每个实例依据地图版本选择表，不修改全局 `CHAPTER=130`，同进程处理新旧日志互不切换全局数据。

生成命令：

```powershell
python -m release.NameGenerator --resources equip/resources/cangshengtf --output replayer/NameCangsheng.py
python -m unittest discover -s tests -p test_buff_tables.py -v
```

## 生成规则修复

1. 按列名读取 `ID`、`Level` 与 `BeginAttribN/BeginValueNA`，保留精确技能等级；`Active`、`EndTime` 是周期/结束动作，不冒充全程常驻增益。
2. 同一 Buff 内重复出现的属性求和，保留小数；已识别属性含非数值时明确报错。后一张表即便为空效果，也会覆盖前一张同 ID/等级记录。
3. 伤害吸收使用明确属性名单。`atDamageAbsorbShieldCoefficient` 是盾增效、`atGlobalTherapyAbsorb` 是治疗吸收，均不再误当伤害盾。
4. 新减伤表区分外功、阳性、混元、阴性、毒性，避免单系减伤套用所有伤害。旧全系兼容表仅收录五系数值相同的正减伤。
5. 新增全类型攻击/破防/会效与各内功防御的属性别名。
6. 梅花三弄 `9336.1` 当前已无直接减伤，源表改为调用 `33086.30`。`33086` 的表名是“主角技能可共存实际减伤”，春泥、天地、山等多个技能也共用该 ID；运行时使用实际子 Buff 的等级和日志来源，不能把所有 `33086` 都归给梅花盾施放者。

当前生成规模：69,402 个名称、14,843 个属性增益、1,581 个伤害盾、1,305 个全系兼容减伤、3,432 个分系减伤/易伤记录、3,074 个治疗相关属性记录。

## 8.16.0 核心表更新与专项验证

- 替换前已备份 14 份核心表、`source_manifest.json` 和 `replayer/NameCangsheng.py`，共 16 份。备份位置为 `backups/release-8.16.0/before-data`；每份逐字节校验通过，大小、时间、SHA256 保存在 `backup_manifest.json`。
- 14 份源表全部匹配 0920 报告清单；其中 11 份内容发生变化，`buff2.tab`、`enchant.tab`、`Set.tab` 与原版本相同。仅替换清单中的原始核心表，未改历史赛季源表。
- 重新生成相对发布前增加属性增益 38 条、变更 24 条；治疗相关记录增加 22 条、变更 19 条。名称增加 20 条、移除 2 条、变更 1 条；盾增加 2 条；全系和分系减伤各增加 1 条。
- 秋肃 `29294.1`、左旋右转 `20938.1`、庄周梦 `23543.1` 均为 `atStrainBase=7`，每层无双等级 +7。三者只有等级 1，没有其他 Begin/Active/EndTime 属性，移动端表也没有同 ID 覆盖。不能与同名易伤、减伤或计数 Buff 混用。
- 保留的 `xinfa_buff_evidence.json` 仍标记 9503：其中 72 条转换和 30 条会效记录，与 9506 完整源行及显示文本逐项一致；没有通过改写旧文件哈希来冒充重新取证。全表哈希因其他行变化而不同，旧证据的 `--check` 全表哈希检查仍描述原快照；当前兼容性由 `test_cangsheng_xinfa.py` 的完整证据对象比较验证。
- 生成结果与重新构建的字典一致；18 项增益/实例回归、6 项心法证据回归、11 项装备回归通过。更新明细保存在 `backups/release-8.16.0/data-update-validation.json`。

## 61 份现场 JCL 覆盖核验（9503 历史报告）

JCL 已由主流程先压缩备份并校验 SHA256。以下为先前以 9503 源表读取 61 份原日志 Buff 事件的结果，详见同目录 `buff-coverage.json`；该历史报告本次没有覆盖或改标为 9506：

- 共 2,293,867 条 Buff 事件、1,017 个 ID/等级组合。
- 1,016 个真实组合全部精确命中当时的 9503 源表，不需要等级 0 回退。
- 唯一未收录组合为 `2,0,0`（17,652 条占位记录），没有猜测其增益效果。
- 已识别的现场组合包含属性增益 154 种、分系减伤/易伤 28 种、伤害盾 13 种、治疗相关属性 26 种；与源行生成结果的差异为 0。
- 通用减伤子 Buff `33086` 的现场等级 10、15、20、25、30、35、40、45、50、55、60、70、80 均精确收录，包括梅花使用的等级。
- 首份突利和顺 JCL 的单系伤害结果与解包 `skills.tab/KindType` 交叉核验：`fullResult[0..4]` 分别为外功、阳性、混元、阴性、毒性，代表技能分别为 24009、3848、36438、6554、28081。

### 实例与有效标记的二次核验

插件作者的 [JCL 数据结构文档](https://github.com/tinymins/JX3MY/wiki/MY-Combat-Log-DS#buff-刷新-buff_update) 将 Buff 事件第 3 字段定义为 `nIndex`、第 11 字段定义为 `bIsValid`。现场确实有同一实例层数仍为 1、有效标记却由 true 转为 false，直到数秒后才删除的情况；不能只看层数。

- 新季解析保留实例槽位及有效标记，减伤与盾按槽位保存；缺少槽位的旧数据格式退到“ID、等级、来源”的组合。不同来源或槽位的同 ID/等级事件不会互相覆盖，移除只影响相应实例。
- 原始事件层数保持不变，机制时间线仍可使用原日志。新季数值统计对 `bIsValid=false` 的实例立即停止计入；原有删除事件的短延迟与梅花盾黏着规则保留。旧季仍使用原统计逻辑。
- 61 份日志均出现非零层数但失效的 `33086`，合计 13,902 条；45 份出现同等级、多槽位且标记有效的事件状态。这里是原始事件顺序的诊断统计，不能把同帧短时并存直接当作已验证的实际减伤总量。
- 内部实例键不会进入展示层，增益明细仍使用原始 `full_id` 查技能名。日志未给出可靠施放者时，不替换成其他人的贡献。
- 专项回归 14 项通过：涵盖同等级多来源/槽位、单个移除、失效及恢复、盾来源保留、规范技能名称、精确等级、旧季兼容及下述伤害计算边界。

## 已支持的统计与明确边界

- 实际伤害、实际有效治疗、过量治疗和 JCL 记录的伤害吸收继续使用真实日志数值。
- rHPS 仍按项目既有的有效治疗、伤害吸收及减伤等效治疗口径计算，不等于“治疗增益来源归属”指标。
- `THERAPY_DICT` 已保存治疗基础值、治疗百分比、造成治疗、受到治疗与盾增效等词条。它尚未接入治疗增益归属重算，不能据此声称所有治疗增益贡献已正确分摊。治疗吸收、技能脚本动态增益及服务器隐藏逻辑也不由静态底表自动推导。
- 新减伤结算按伤害系别使用项目既有的加法等效治疗模型。减伤和易伤都保留符号参与分母，但只给正减伤项记贡献，避免易伤被错误计成正减伤。总减伤大于或等于 100% 时，无法从剩余伤害反推原伤害，跳过反推，不生成负数、无穷大或伪造贡献。
- 完全被吸收而没有伤害系别的事件，只有所有当前效果都五系相同才可归属；存在单系减伤/易伤就跳过该笔反推。混合系别伤害无法得知每系吸收量时，仅计算已明确记录的伤害部分。没有新增无法验证的吸收量分配假设。
- JCL 单次伤害吸收量缺少逐盾分摊数据，继续沿用项目现有的单盾来源选择口径；一击同时消耗多个盾时不能据此声称已精确还原每个盾的贡献。
- 装备源现已隔离读取新赛季 14 份表中的装备数据；物品词条读取扩展到 `Magic16Type`，不会因中间空槽丢掉后面的词条。`AttributeDisplay.GetRawEquipmentFeature` 提供原始物品词条，未知新物品明确报错，不返回伪造的全零面板。

## 50 级属性与 rDPS（beta.2）

50 级系数、31 心法原始技能、装备转换及 25 人普通洛阳之战 NPC 防御现已接通，详情见[属性对齐与通关rDPS](属性对齐与通关rDPS.md)。原始 Lua 证实等级参数为 50 级 990；GlobalParam 给出会心 9512.91、会效 3504.6、加速 10107.9 等分母。

beta.2 完成时生成表包含 14,805 个增益条目，8.16.0 核心表刷新后为 14,843 条。已补充主属性/治疗/气血/拆招转换、全能、非侠士伤害与不封顶会效词条；重复属性仍累加并按精确等级选取。`atTherapyPowerBasePercentAdd` 已审计原表没有实例，不根据字段名猜测乘区。

新版计算完全使用当前装备原始 at* 数值，不读取旧版心法数值。基础乘区与心法转换分开，治疗/气血等二级转换按依赖顺序计算；指定内功攻击转治疗读取对应学校攻击，而非本次事件学校。临时 T 转换直接取实际 buff ID/等级的原值除 1024，不再次压缩或把一仇 buff 乘三。

增益槽位的生效、失效、刷新、到期与删除已独立处理；删除旧槽位不会移除同 ID 的存活实例。根据日志结束帧到期，真实实例替换开战前推定副本。不同来源的同名有效实例独立归功，导出时保持原始技能名。

`AttributeDisplay` 对实际装备返回 50 级面板；空字符串或全零槽位返回缺失。目标 NPC 防御与装备有缺口时仍标记 `incomplete`，UI 保留待校准显示，并阻止把缺失数据作为有效成绩上传。

当前启用范围为已验证的 25 人普通洛阳之战；默认裸属性、脚本附魔触发、治疗增益归属及人工游戏面板比对的边界见上述报告。
